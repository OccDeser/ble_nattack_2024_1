\documentclass[border=2pt]{standalone}
\usepackage{amsmath}

\begin{document}
% security_protocol_theory := 'theory' ident 'begin' body 'end'
% body := (signature_spec | global_heuristic | tactic | rule |
%             restriction | lemma | formal_comment)*
% rule        := 'rule' ident  ':' [let_block]
%                    '[' [facts] ']' ( '-->' | '--[' facts ']->') '[' [facts] ']'
% let_block   := 'let' (msg_var '=' msetterm)+ 'in'
% facts := fact (',' fact)*
% fact  := ['!'] ident '(' [msetterm (',' msetterm)*] ')' 

$\begin{array}{rcl}
    \textbf{LIN SHI ZHAN WEI} & ::= & \text{LIN SHI ZHAN WEI LIN SHI ZHAN WEI LIN SHI ZHAN WEI LIN SHI ZHAN WEI} \\
    \textbf{Theory}\ \boldsymbol{T} & ::= & \text{theory}\ \boldsymbol{n_T} \ \textsf{begin}\ \boldsymbol{body} \ \text{end} \\
    \textbf{Body}\ \boldsymbol{body} & ::= & \left( \boldsymbol{signature\_spec} \mid \boldsymbol{global\_heuristic} \mid \boldsymbol{tactic} \mid \boldsymbol{rule} \mid \boldsymbol{restriction} \mid \boldsymbol{lemma} \mid \boldsymbol{formal\_comment} \right)^+ \\
    \textbf{Rule}\ \boldsymbol{rule} & ::= & \text{rule}\ \boldsymbol{n_{rule}} \ \text{:}\ [\boldsymbol{let\_block}] \\
    && [\boldsymbol{facts}] \ \text{--}\boldsymbol{>} \ \text{[}\boldsymbol{facts}\text{]} \\
    \textbf{Let Block}\ \boldsymbol{let\_block} & ::= & \text{let}\ \left( \boldsymbol{msg\_var} = \boldsymbol{msetterm} \right)^+ \ \text{in} \\
    \textbf{Facts}\ \boldsymbol{facts} & ::= & \boldsymbol{fact} \ \left( \text{,}\ \boldsymbol{fact} \right)^* \\
    \textbf{Fact}\ \boldsymbol{fact} & ::= & [\text{!}]\ \boldsymbol{n_f} \text{(} [\boldsymbol{msetterm} \text{(,}\ \boldsymbol{msetterm}\text{)}^* \text{)} 
\end{array}$

% $\begin{array}{rcl}
% \textbf{Contract}\ C & ::= & \texttt{contract}\ n_c \{ d, \texttt{func}^+ \} \\
% \textbf{Func}\ \texttt{func} & ::= & \texttt{function}\ f(d) \{ \texttt{stmt} \} \\
% \textbf{Stmt}\ \texttt{stmt} & ::= & \texttt{stmt}_A \mid \texttt{if}\ e_b \ \texttt{then}\ \texttt{stmt} \ \texttt{else}\ \texttt{stmt} \mid \texttt{stmt}; \ \texttt{stmt} \\
% \textbf{Atom Stmt}\ \texttt{stmt}_A & ::= & v \leftarrow e \mid \tau v \leftarrow e \mid \texttt{require}\ e_b \mid e_c \mid \texttt{return} \\
% \textbf{Bool Expr}\ e_b & ::= & e == e \mid e < e \mid e > e \mid e! = e \\
% \textbf{Call Expr}\ e_c & ::= & v.\texttt{transfer}(v) \mid v.\texttt{send}(v) \mid v.\texttt{call}().\texttt{value}(v) \mid n_c(v).f(p) \\
% \textbf{Expr}\ e & ::= & v \mid v + e \mid v - e \mid v * e \mid v / e \mid v \% e \\
% \textbf{Type}\ \tau & ::= & \tau_B \mid \tau_B \rightarrow \tau \mid n_c \\
% \textbf{BasicType}\ \tau_B & ::= & \texttt{uint} \mid \texttt{bool} \mid \texttt{address}
% \end{array}$

\end{document}
